# Repository

Este UserRepository es una pieza central en la arquitectura de un backend con Spring Boot y Spring Data JPA, porque es el componente que conecta tu aplicación con la base de datos sin que tengas que escribir SQL manualmente.

## Optional

Optional<User>
Este UserRepository es una pieza central en la arquitectura de un backend con Spring Boot y Spring Data JPA, porque es el componente que conecta tu aplicación con la base de datos sin que tengas que escribir SQL manualmente.

Evita errores como:
Evita errores como:
